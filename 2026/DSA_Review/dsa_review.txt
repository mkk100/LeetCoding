Big O notation
Time & Space Complexity

Static Array (Mutable)
Search - O(N)
Insertion - O(N)
Deletion - O(N)

Dynamic Array 
called List in Python, Python doesn't even use static array.
Static array under the hood.
Insertion - O(N)
Append - O(N) or O(1), depends on capacity
Deletion - O(N)

amortized means on average

Strings
strings are immutable in Python

Singly Linked List (non-contiguous)
Search, Insertion, Deletion (all O(n))
Doubly Linked List

Hashtable
Hash Function - process that takes input and convert it to index
Collisions - same index
Collision handling - separate chaining and linear probing

hash set - store unique items
hash map - dict (kv pairs)

Stacks 
LIFO - add and pop from the right side, all ops O(1)

Queues
FIFO
enqueue - adds from right
dequeue - remove from left O(N)
if you use deque than O(1), cus deque library uses LL.

Recursion
space complexity - O(N)
time - depends

Binary Search

Traditional
m = (l + r) // 2, we round it down so if even number, we pick lower one.

Over-under
Condition based

Binary Trees
Terms
parent, child
leaf nodes
root
Trees are sub-category of graph
perfect tree - everything filled up
complete tree - last might not be filled.

i, left child is 2i, right child 2i + 1

DFS - stack
BFS - queue

BST (Binary Search Tree) - left smaller than parent, right bigger than parent
hence TC: O(log N)

Heaps or PQ
- Type of Binary Tree
2i+1 (left), 2i+2 (right)

heapify - O(n)
heap pop - O(log n)
heap push - O(log n)
heap peek - O(1)

Sorting
bubble sort - TC O(n^2), SC O(1)
insertion sort - TC O(n^2), SC O(1)
selection sort - TC O(n^2), SC O(1)
merge sort - O(NlogN), SC O(logN)
quick sort - avg case O(nlogN), good pivot
counting sort - TC (O(k+n)), roughly o(N)for  

Graphs
Trees and LLs are subclasses of graphs
Graph has vertices (or nodes) and edges (connections).

Directed graph (have directions)
Undirected graph
Graph can have cycles

Three ways to represent matrices
- Edge List (hard to work with)
- Adjacency Matrix (2D list, a lot of 0s)
- Adjacency List (hashmap, key is the node's value, usually this is the way)

Traversal
DFS - recursive and stack
BFS 

TC - O(V + E), for each vertex and its related edges
SC - O(V + E)

Trees - connected acyclic graph
for trees,
V = nodes
E = n - 1

Backtracking - exhaustive search for all the solutions
